//React Native
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 24,
    },
    errorText: {
        color: "red",
        fontSize: 18,
        fontWeight: 'bold',
        textAlign: 'center',
    },
    submitButtonContainer: {
        flex: 1,
         marginTop: 12
    }
})